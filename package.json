{
  "name": "multi-launcher-for-claude-desktop",
  "version": "1.0.0",
  "description": "Launcher for running several Claude Desktop accounts side by side on Windows",
  "author": "Sumant Sagar",
  "license": "MIT",
  "homepage": "https://github.com/SumantSagar73/multi-launcher-for-claude-desktop#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SumantSagar73/multi-launcher-for-claude-desktop.git"
  },
  "bugs": {
    "url": "https://github.com/SumantSagar73/multi-launcher-for-claude-desktop/issues"
  },
  "keywords": [
    "claude",
    "claude-desktop",
    "windows",
    "multi-account",
    "launcher",
    "electron"
  ],
  "main": "src/main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "test": "node --test test/*.test.js",
    "package": "node scripts/make-icon.js && electron-packager . \"Multi Launcher for Claude Desktop\" --platform=win32 --arch=x64 --out=dist --overwrite --icon=build/icon.ico --ignore=\"^/(dist|release|stage|test|scripts|build|spike-profile-a|.claude)($|/)\" --ignore=\"^/README.md$\"",
    "release": "node scripts/release.js",
    "install-app": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/install.ps1",
    "uninstall-app": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/uninstall.ps1"
  },
  "build": {
    "appId": "io.github.sumantsagar73.multilauncher",
    "productName": "Multi Launcher for Claude Desktop",
    "artifactName": "Multi-Launcher-for-Claude-Desktop-${version}.${ext}",
    "directories": {
      "output": "release",
      "buildResources": "build"
    },
    "win": {
      "icon": "build/icon.ico",
      "signAndEditExecutable": false,
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "zip",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "nsis": {
      "oneClick": true,
      "perMachine": false,
      "allowElevation": false,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Multi Launcher for Claude Desktop",
      "deleteAppDataOnUninstall": false,
      "runAfterFinish": true
    }
  },
  "devDependencies": {
    "@electron/packager": "^20.3.0",
    "electron": "^44.4.5",
    "electron-builder": "^26.0.0"
  }
}
