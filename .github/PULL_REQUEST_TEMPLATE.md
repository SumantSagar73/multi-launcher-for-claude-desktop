## What changed and why

## How I tested it

<!-- Unit tests added or updated? What did you check by hand, and against which data folder (CML_USER_DATA)? -->

## Checklist

- [ ] `npm test` passes and I ran `node --check` on the files I touched
- [ ] Nothing in this change writes to, closes or restyles the Default Claude account
- [ ] No sign-in tokens, cookies or one-time codes are read, stored or logged
- [ ] I tested against a scratch data folder, not my real accounts
- [ ] `CHANGELOG.md` is updated if users would notice the change
